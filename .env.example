# Local development only. For Neon, replace with its pooled connection URL,
# keeping the SSL settings supplied by Neon. Never commit .env.local.
DATABASE_URL=postgresql://gametime:gametime@127.0.0.1:5433/gametime
# Optional separate direct connection for migrations; defaults to DATABASE_URL.
# DATABASE_URL_UNPOOLED=
